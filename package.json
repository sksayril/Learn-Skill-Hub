{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=artifacts/skill-mission-india",
    "build": "npm run build --workspace=artifacts/skill-mission-india",
    "start": "npm run start --workspace=artifacts/skill-mission-india",
    "typecheck": "npm run typecheck --workspace=artifacts/skill-mission-india"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
